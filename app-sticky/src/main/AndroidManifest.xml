<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.alea.tuto.service_kill.sticky">

    <application
        android:name="net.alea.tuto.service_kill.sticky.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">

        <activity android:name="net.alea.tuto.service_kill.sticky.MyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="net.alea.tuto.service_kill.sticky.MyService"/>

    </application>

</manifest>